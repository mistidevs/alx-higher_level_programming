#!/usr/bin/python3
def element_at(my_list, idx):
    if idx < 0:
        return None
    len = len(my_list)
    if idx > (len - 1):
        return None
    for i in my_list:
        if i == idx:
            return my_list[i]


if __name__ == '__main__':
    element_at()
